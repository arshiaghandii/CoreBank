# پلن ادامه پروژه CoreBank

## هدف ۹۰ روزه
تبدیل CoreBank از یک هسته بانکی عملیاتی به یک پلتفرم **قابل‌اتکا، مانیتورشونده و توسعه‌پذیر** با قابلیت یکپارچه‌سازی بیرونی.

---

## فاز ۱ (هفته ۱ تا ۲): تثبیت پایه و کاهش ریسک

### 1) استانداردسازی کیفیت کد
- پوشش تست سرویس‌های اصلی: `AccountService`، `TransactionService`، `LoanService`.
- اضافه‌کردن تست‌های یکپارچه برای APIهای حیاتی (ایجاد حساب، انتقال وجه، بازپرداخت وام).
- تعریف حداقل پوشش تست هدف: 70٪ برای لایه سرویس.

### 2) سخت‌سازی مدیریت خطا
- یکپارچه‌سازی پاسخ خطا در `GlobalExceptionHandler` (کد، پیام، زمان، مسیر).
- تعریف خطاهای دامنه‌ای مشخص برای سناریوهای بانکی پرتکرار.

### 3) بازبینی امنیت
- بررسی مسیرهای حساس در `SecurityConfig` و تفکیک سطح دسترسی admin/user.
- اضافه‌کردن محدودیت نرخ (Rate Limiting) برای endpointهای حساس به brute force.

**خروجی فاز ۱:** نسخه پایدار v1.1 با تست و امنیت پایه بهتر.

---

## فاز ۲ (هفته ۳ تا ۵): شروع یکپارچه‌سازی خارجی

### 4) سرویس تبدیل ارز (REST Client)
- پیاده‌سازی `CurrencyExchangeService` با `WebClient`.
- endpoint جدید برای نمایش معادل ارزی موجودی (IRR → USD/EUR).
- کش نرخ ارز برای 1 تا 5 دقیقه جهت کاهش وابستگی به API بیرونی.
- fallback در زمان قطعی API (نمایش آخرین نرخ معتبر + هشدار stale data).

### 5) مشاهده‌پذیری پایه
- لاگ ساختاریافته برای عملیات مالی مهم (create/transfer/loan).
- افزودن correlation-id برای ردگیری درخواست بین لایه‌ها.

**خروجی فاز ۲:** قابلیت نرخ ارز آنلاین + قابلیت دیباگ بهتر عملیات.

---

## فاز ۳ (هفته ۶ تا ۸): معماری رویدادمحور (Async)

### 6) اطلاع‌رسانی غیرهمگام با RabbitMQ
- افزودن RabbitMQ به `docker-compose`.
- انتشار event پس از انتقال موفق (`TransferSucceededEvent`).
- ساخت consumer برای ارسال نمادین SMS/Email.
- پیاده‌سازی retry + dead-letter queue برای خطاهای ارسال.

### 7) پایداری تراکنش و رویداد
- بررسی الگوی Outbox برای جلوگیری از ناسازگاری بین DB و Queue.

**خروجی فاز ۳:** کاهش latency APIهای مالی و آماده‌سازی برای مقیاس‌پذیری.

---

## فاز ۴ (هفته ۹ تا ۱۰): اتوماسیون عملیات بانکی

### 8) واریز حقوق گروهی (Spring Integration)
- ساخت flow برای خواندن فایل `salary.csv` از پوشه ورودی.
- اعتبارسنجی رکوردها (شماره حساب، مبلغ، تکراری نبودن).
- ثبت گزارش نتیجه (موفق/ناموفق) در فایل output یا جدول لاگ.

**خروجی فاز ۴:** ورود فایل و انجام خودکار عملیات حجیم بدون دخالت دستی.

---

## فاز ۵ (هفته ۱۱ تا ۱۲): مانیتورینگ و عملیات تولید

### 9) Actuator + HealthIndicator سفارشی
- فعال‌سازی `health`, `metrics`, `prometheus`.
- HealthIndicator برای Redis و Keycloak.

### 10) Admin + Docker Production
- راه‌اندازی Spring Boot Admin برای مانیتورینگ گرافیکی.
- بهینه‌سازی Dockerfile به Multi-stage build.
- اجرای یکپارچه App + DB + Redis + Keycloak + RabbitMQ + Admin.

**خروجی فاز ۵:** محیط production-ready با پایش عملیاتی قابل اتکا.

---

## بک‌لاگ اختیاری (بعد از ۹۰ روز)
- داشبورد زنده تراکنش‌ها با SSE و WebFlux.
- ارتباط شعب با RSocket (Fire-and-Forget).
- مهاجرت تدریجی به معماری ماژولار/میکروسرویس برای بخش‌های پرمصرف.

---

## KPIهای پیشنهادی برای سنجش پیشرفت
- میانگین زمان پاسخ انتقال وجه: < 300ms (بدون وابستگی ارسال اعلان).
- نرخ خطای APIهای حیاتی: < 1٪.
- پوشش تست لایه سرویس: ≥ 70٪.
- MTTR خطاهای تولید: کاهش حداقل 30٪ نسبت به وضعیت فعلی.

---

## ترتیب اجرای عملی (اولویت واقعی)
1. تست + امنیت + خطا (فاز ۱)
2. تبدیل ارز (فاز ۲)
3. رویدادمحوری و اعلان async (فاز ۳)
4. واریز گروهی (فاز ۴)
5. مانیتورینگ و دیپلوی تولید (فاز ۵)

این ترتیب باعث می‌شود ابتدا ریسک سیستم پایین بیاید، بعد قابلیت‌های جدید اضافه شود، و در نهایت آماده تولید واقعی شوید.
